export const printLine = (line) => {
  console.log('===> FROM THE PRINT MODULE:', line);
};

// chrome.tabs.query({ currentWindow: true, active: true }, function (tabs) {
//   console.log('sss', tabs);
// });
